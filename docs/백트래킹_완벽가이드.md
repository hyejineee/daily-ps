# 백트래킹 완벽 가이드

> 스타트와 링크 문제를 풀면서 배운 내용 정리 (2026.02.09)

## 목차
1. [조합 vs 순열 핵심 차이](#조합-vs-순열-핵심-차이)
2. [조합 패턴 (가장 중요!)](#조합-패턴-외울-것)
3. [흔한 실수와 해결](#흔한-실수와-해결)
4. [문제 유형별 접근법](#문제-유형별-접근법)
5. [백트래킹 설계 체크리스트](#백트래킹-설계-체크리스트)

---

## 조합 vs 순열 핵심 차이

### 조합 (Combination) - 순서 무관 ⭐
```javascript
// [1,2]와 [2,1]은 같은 것으로 취급
// 예: 팀 나누기, 대표 선출

const combination = (start, selected) => {
    if (selected.length === r) {
        result.push([...selected]);
        return;
    }

    for (let i = start; i < n; i++) {      // ✅ start부터 시작
        selected.push(i);                   // 또는 arr[i]
        combination(i + 1, selected);       // ✅ i + 1 전달
        selected.pop();
    }
};

// 호출: combination(0, [])
```

**결과 예시 (N=4, r=2)**:
```
[0,1], [0,2], [0,3], [1,2], [1,3], [2,3]  // 총 6개 (4C2)
```

### 순열 (Permutation) - 순서 중요
```javascript
// [1,2]와 [2,1]은 다른 것
// 예: 순서 배치, 암호 생성

const visited = Array(n).fill(false);

const permutation = (selected) => {
    if (selected.length === r) {
        result.push([...selected]);
        return;
    }

    for (let i = 0; i < n; i++) {          // ✅ 매번 0부터
        if (!visited[i]) {                  // ✅ visited 체크 필수
            visited[i] = true;
            selected.push(i);
            permutation(selected);          // i 전달 안 함
            selected.pop();
            visited[i] = false;
        }
    }
};

// 호출: permutation([])
```

**결과 예시 (N=4, r=2)**:
```
[0,1], [0,2], [0,3], [1,0], [1,2], [1,3],
[2,0], [2,1], [2,3], [3,0], [3,1], [3,2]  // 총 12개 (4P2)
```

---

## 조합 패턴 (외울 것!)

### 패턴 1: 인덱스로 조합 (스타트와 링크)

**문제**: N명을 두 팀으로 나누기

```javascript
const solution = (n, stats) => {
    let minDiff = Infinity;

    // 팀의 능력치 계산
    const calcTeamPower = (team) => {
        let sum = 0;
        for (let i = 0; i < team.length; i++) {
            for (let j = i + 1; j < team.length; j++) {
                sum += stats[team[i]][team[j]] + stats[team[j]][team[i]];
            }
        }
        return sum;
    };

    const backtrack = (start, team) => {
        // 종료 조건: N/2명 선택 완료
        if (team.length === n / 2) {
            // 나머지 팀 구하기
            const otherTeam = [];
            for (let i = 0; i < n; i++) {
                if (!team.includes(i)) {
                    otherTeam.push(i);
                }
            }

            const power1 = calcTeamPower(team);
            const power2 = calcTeamPower(otherTeam);
            minDiff = Math.min(minDiff, Math.abs(power1 - power2));
            return;
        }

        // 조합 생성
        for (let i = start; i < n; i++) {
            team.push(i);                // i번 사람 선택
            backtrack(i + 1, team);      // 다음 사람부터 탐색
            team.pop();                  // 복구
        }
    };

    backtrack(0, []);
    return minDiff;
};
```

### 패턴 2: 값으로 조합 (배열 요소 선택)

**문제**: 배열에서 2개를 선택하여 합이 target

```javascript
const arr = [10, 20, 30, 40];
const target = 50;

const findCombinations = (start, selected) => {
    if (selected.length === 2) {
        const sum = selected.reduce((a, b) => a + b, 0);
        if (sum === target) {
            console.log(selected);  // [10, 40], [20, 30]
        }
        return;
    }

    for (let i = start; i < arr.length; i++) {
        selected.push(arr[i]);           // 값 저장
        findCombinations(i + 1, selected);
        selected.pop();
    }
};

findCombinations(0, []);
```

---

## 흔한 실수와 해결

### 실수 1: start를 쓰지 않음 ❌

```javascript
// ❌ 잘못된 코드
for (let i = 0; i < n; i++) {  // 매번 0부터 시작
    selected.push(i);
    backtrack(selected);  // 중복 발생: [1,1], [2,2], ...
}

// ✅ 올바른 코드
for (let i = start; i < n; i++) {  // start부터 시작
    selected.push(i);
    backtrack(i + 1, selected);  // i + 1 전달
}
```

**문제점**: [1,2]와 [2,1]이 모두 생성되어 중복 발생

### 실수 2: 4중 반복문 사용 ❌

```javascript
// ❌ 백트래킹이 아님! O(n^4) → 시간 초과
for (let i = 0; i < n; i++) {
    for (let j = 0; j < n; j++) {
        for (let k = 0; k < n; k++) {
            for (let l = 0; l < n; l++) {
                // 무한 루프 위험
            }
        }
    }
}

// ✅ 재귀로 선택 - O(2^n) 또는 O(n!)
const backtrack = (idx, selected) => {
    if (selected.length === target) return;
    if (idx === n) return;

    // 선택 O
    selected.push(idx);
    backtrack(idx + 1, selected);
    selected.pop();

    // 선택 X
    backtrack(idx + 1, selected);
};
```

### 실수 3: 무엇을 선택하는지 불명확 ❌

**잘못된 이해**: "stats 배열의 값을 선택하는 거야!"
```javascript
// ❌ 능력치 값을 선택?
arr1.push(stats[i][j]);
arr2.push(stats[i2][j2]);
```

**올바른 이해**: "N명의 사람(인덱스)를 선택하는 거야!"
```javascript
// ✅ 사람을 선택!
team.push(i);  // i번 사람 선택
```

---

## 문제 유형별 접근법

| 문제 유형 | 패턴 | 시간 복잡도 | 예시 문제 |
|-----------|------|-------------|----------|
| **N명 중 M명 선택** | 조합 | O(nCm) | 스타트와 링크, 치킨 배달 |
| **순서대로 배치** | 순열 | O(n!) | N과M, 암호 생성 |
| **격자 탐색** | DFS + 백트래킹 | O(4^(n×m)) | 알파벳, N-Queen |
| **부분집합** | 조합 (가변) | O(2^n) | 부분수열의 합 |

### 언제 조합? 언제 순열?

| 질문 | 조합 | 순열 |
|------|------|------|
| **순서 중요?** | ❌ | ✅ |
| **[1,2]와 [2,1]** | 같음 | 다름 |
| **팀 나누기** | ✅ | ❌ |
| **1등/2등/3등** | ❌ | ✅ |

---

## 백트래킹 설계 체크리스트

### 1단계: 문제 분석
- [ ] **무엇을 선택?** (인덱스 / 값 / 좌표)
- [ ] **몇 개 선택?** (고정 개수 / 가변)
- [ ] **순서 중요?** (Yes → 순열, No → 조합)
- [ ] **중복 허용?** (Yes → 중복조합/순열)

### 2단계: 종료 조건 설정

```javascript
const backtrack = (/* 상태 */) => {
    // ✅ 필수 1: 성공 조건
    if (선택 완료) {
        결과 처리;
        return;
    }

    // ✅ 필수 2: 실패/불가능 조건
    if (더 이상 선택 불가) {
        return;
    }

    // ✅ 선택적 3: 가지치기
    if (불가능한 경로) {
        return;
    }

    // 선택 반복
    for (...) {
        // 선택 → 재귀 → 복구
    }
};
```

### 3단계: 시간 복잡도 예측

| 복잡도 | N 범위 | 1초 기준 |
|--------|--------|---------|
| O(2^n) | n ≤ 20 | 2^20 = 1,048,576 |
| O(n!) | n ≤ 10 | 10! = 3,628,800 |
| O(nCr) | 상황마다 다름 | 계산 필요 |
| O(n^4) | n ≤ 100 | ⚠️ 백트래킹 아님 |

### 4단계: 가지치기 (선택적)

```javascript
// 예: 현재 최솟값보다 큰 경우 탐색 중단
if (currentSum > minValue) return;

// 예: 남은 선택으로 목표 달성 불가능
if (remaining + selected.length < target) return;
```

---

## 나머지 팀 구하는 법 (조합의 여집합)

### 방법 1: includes 사용 (간단)
```javascript
const otherTeam = [];
for (let i = 0; i < n; i++) {
    if (!team.includes(i)) {
        otherTeam.push(i);
    }
}
```

### 방법 2: visited 배열 사용 (효율적)
```javascript
const visited = Array(n).fill(false);

// 선택된 팀 표시
team.forEach(member => visited[member] = true);

// 나머지 팀 구하기
const otherTeam = [];
for (let i = 0; i < n; i++) {
    if (!visited[i]) {
        otherTeam.push(i);
    }
}
```

---

## 빠른 참고 공식 ⭐

### 조합 (가장 많이 사용)
```javascript
const combination = (start, selected) => {
    if (selected.length === r) {
        result.push([...selected]);
        return;
    }

    for (let i = start; i < n; i++) {
        selected.push(i);         // 또는 arr[i]
        combination(i + 1, selected);
        selected.pop();
    }
};
```

### 순열
```javascript
const visited = Array(n).fill(false);

const permutation = (selected) => {
    if (selected.length === r) {
        result.push([...selected]);
        return;
    }

    for (let i = 0; i < n; i++) {
        if (!visited[i]) {
            visited[i] = true;
            selected.push(i);
            permutation(selected);
            selected.pop();
            visited[i] = false;
        }
    }
};
```

---

## 디버깅 팁

### 1. 선택 과정 출력
```javascript
const backtrack = (start, selected) => {
    console.log("현재 선택:", selected, "다음 시작:", start);
    // ...
};
```

### 2. 작은 예제로 검증
```javascript
// N=4일 때 손으로 그려보고 코드 결과와 비교
// [0,1], [0,2], [0,3], [1,2], [1,3], [2,3]
```

### 3. 종료 조건 체크
```javascript
// return이 제대로 동작하는지 확인
if (selected.length === target) {
    console.log("종료!");
    return;  // 여기서 멈추는지 확인
}
```

---

## 연습 문제

### 기초
- ✅ **스타트와 링크** (#14889) - 조합 기본
- 부등호 (#2529) - 순열 기본
- N과M 시리즈 - 조합/순열 연습

### 중급
- 치킨 배달 (#15686) - 조합 + 구현
- 암호 만들기 (#1759) - 조합 + 정렬
- ABCDE (#13023) - DFS + 백트래킹

### 고급
- N-Queen (#9663) - 백트래킹 대표 문제
- 알파벳 (#1987) - 격자 + 백트래킹
- 스도쿠 (#2580) - 백트래킹 + 구현

---

## 요약: 절대 잊지 말 것!

1. **조합**: `i = start`부터, 재귀에 `i + 1` 전달
2. **순열**: `i = 0`부터, `visited` 배열 사용
3. **4중 반복문 ❌** → 재귀로 선택 ✅
4. **무엇을 선택하는가** 명확히 (인덱스 vs 값)
5. **종료 조건 2가지** 필수 (성공, 실패)
6. **시간 복잡도** 먼저 계산하기

---

📅 **작성일**: 2026.02.09
📝 **참고 문제**: 백준 14889 - 스타트와 링크
